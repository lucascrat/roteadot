services:
  roteador:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - roteador_data:/app/data
    environment:
      - ADMIN_PASSWORD=M3uPro@2026!
      - SESSION_TIMEOUT_MINUTES=30
      - DB_PATH=data/roteador.db
    restart: unless-stopped

volumes:
  roteador_data:
